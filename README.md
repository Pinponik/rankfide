# FIDE Elo Rating Calculator

A simple calculator written in Rust.

Just download, (you can use this:) and use.

```
cargo run --release # or `cargo run` # ! needs `cargo`!
mv target/release/rankfide.exe ..
mv probabilities.csv ..
mv initial.csv ..
```
